steps:
  - name: 'gcr.io/cloud-builders/gcloud'
    args:
      - functions
      - deploy
      - spreadsheet_api
      - --runtime=python312
      - --trigger-http
      - --region=asia-northeast1
      - --allow-unauthenticated
      - --memory=256MB
      - --timeout=60s
      - --env-vars-file=.env.yaml